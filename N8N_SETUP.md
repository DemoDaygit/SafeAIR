# 🔗 Интеграция N8N Webhook для SAFE AIR

Это полное руководство по подключению n8n webhook trigger к сайту SAFE AIR для сбора заявок с формы.

---

## 📋 Оглавление

1. [Предварительные требования](#предварительные-требования)
2. [Шаг 1: Создание N8N Workflow](#шаг-1-создание-n8n-workflow)
3. [Шаг 2: Настройка Webhook Trigger](#шаг-2-настройка-webhook-trigger)
4. [Шаг 3: Подключение webhook URL на сайт](#шаг-3-подключение-webhook-url-на-сайт)
5. [Шаг 4: Обработка данных в N8N](#шаг-4-обработка-данных-в-n8n)
6. [Шаг 5: Тестирование](#шаг-5-тестирование)
7. [Структура данных JSON](#структура-данных-json)
8. [Troubleshooting](#troubleshooting)

---

## Предварительные требования

- ✅ Активный аккаунт n8n (self-hosted или n8n.cloud)
- ✅ Доступ к администрированию n8n workflow
- ✅ Доступ к редактированию сайта SAFE AIR
- ✅ Базовое понимание работы webhook

---

## Шаг 1: Создание N8N Workflow

1. **Откройте n8n** и создайте новый workflow
   - Перейдите в n8n dashboard
   - Нажмите `+ New Workflow` или `Create New`

2. **Дайте workflow значащее имя**
   ```
   Название: SAFE AIR - Обработка заявок с сайта
   Описание: Сбор и обработка заявок консультации от клиентов
   ```

3. **Сохраните workflow** (Ctrl+S или нажмите Save)

---

## Шаг 2: Настройка Webhook Trigger

### 2.1 Добавление Webhook узла

1. **Нажмите на "+"** в левом меню или в центре холста
2. **Найдите и выберите** `Webhook` узел (в разделе "Trigger")
3. **Дважды кликните** на узел, чтобы открыть настройки

### 2.2 Конфигурация Webhook

В окне настроек WebHook:

```
📌 Основные параметры:
├── Authentication: None (или установите базовую авторизацию)
├── HTTP Method: POST ✅
├── Path: safe-air (или любой уникальный путь)
└── Response:
    └── Respond when: Workflow executes
    └── Response code: 200
    └── Response body: {"status":"success"}
```

### 2.3 Получение Webhook URL

1. **В узле Webhook нажмите на кнопку** `Copy Webhook URL`
2. **URL будет в формате:**
   ```
   https://your-n8n-instance.com/webhook/safe-air
   ```
   или для n8n cloud:
   ```
   https://your-instance-123.n8n.cloud/webhook/safe-air
   ```

3. **Скопируйте полный URL** - он нам понадобится на шаге 3

### 2.4 Проверка входящих данных

Чтобы увидеть структуру входящих данных:

1. **Сохраните workflow** (даже с одним узлом Webhook)
2. **Активируйте workflow** (toggle в правом верхнем углу)
3. Webhook будет ждать входящих запросов

---

## Шаг 3: Подключение webhook URL на сайт

### Способ 1: Через консоль браузера (быстро)

1. **Откройте сайт SAFE AIR** в браузере
2. **Нажмите F12** чтобы открыть Developer Tools
3. **Перейдите на вкладку Console**
4. **Вставьте команду:**
   ```javascript
   window.setN8NWebhook("https://your-n8n-instance.com/webhook/safe-air")
   ```
   Замените URL на ваш реальный webhook URL из шага 2.3

5. **Нажмите Enter**
6. Вы должны увидеть сообщение: `✅ N8N Webhook URL установлен: https://...`

### Способ 2: Через редактирование HTML (постоянно)

1. **Откройте файл `index.html`** в редакторе
2. **Найдите строку:**
   ```javascript
   webhookUrl: localStorage.getItem('n8n_webhook_url') || 'https://your-n8n-instance.com/webhook/safe-air',
   ```
   Эта строка около 1623 в файле.

3. **Замените URL:**
   ```javascript
   webhookUrl: 'https://your-actual-n8n-instance.com/webhook/safe-air',
   ```

4. **Сохраните файл** и обновите сайт (F5)

### Способ 3: Через переменные окружения (для production)

Если используете Node.js backend или есть serverless функция:

```javascript
const webhookUrl = process.env.N8N_WEBHOOK_URL || 'fallback-url';
```

---

## Шаг 4: Обработка данных в N8N

### 4.1 Парсинг входящих данных

После подключения webhook, добавьте в workflow узел **JSON** для парсинга:

1. **Добавьте узел** `JSON` (обработка → JSON)
2. **Подключите его** к выходу Webhook узла
3. Он автоматически распарсит JSON с сайта

### 4.2 Пример workflow для обработки

Рекомендуемая последовательность узлов:

```
Webhook (входящие данные)
    ↓
JSON (парсинг - опционально)
    ↓
IF (условие на основе данных)
    ├→ SET (добавьте нужные поля)
    └→ Spreadsheet, Email, Database и т.д.
```

### 4.3 Извлечение данных

Чтобы получить доступ к данным в узлах:

```javascript
// Контактная информация
{{ $json.contact.name }}
{{ $json.contact.phone }}
{{ $json.contact.email }}
{{ $json.contact.city }}

// Данные заявки
{{ $json.request.client_type }}        // 'family', 'business', 'public'
{{ $json.request.service_type }}        // 'maintenance', 'repair' и т.д.
{{ $json.request.equipment_count }}     // число

// Метаданные
{{ $json.metadata.timestamp }}
{{ $json.metadata.date_formatted }}
{{ $json.metadata.page_url }}
```

### 4.4 Типичные действия после получения заявки

Вот примеры, что можно делать с заявками:

**📧 Отправить email уведомление**
```
Использовать узел: Email Send
Получатель: support@safe-air.com
Тело письма:
  Новая заявка от {{ $json.contact.name }}
  Телефон: {{ $json.contact.phone }}
  Услуга: {{ $json.request.service_type }}
```

**📊 Сохранить в Google Sheets**
```
Использовать узел: Google Sheets
- Выберите spreadsheet и лист
- Добавьте колонки: Name, Phone, Email, Service, Date, etc.
- Маппируйте поля из JSON
```

**💾 Сохранить в базу данных**
```
Использовать узел: MySQL, PostgreSQL или другой DB
- Создайте таблицу leads (name, phone, email, service_type, client_type, created_at)
- Выполните INSERT запрос с данными из JSON
```

**🔔 Отправить в Telegram**
```
Использовать узел: Telegram Send Message
Формат сообщения:
📞 Новая заявка!
👤 {{ $json.contact.name }}
☎️ {{ $json.contact.phone }}
🏢 Тип: {{ $json.request.client_type }}
🔧 Услуга: {{ $json.request.service_type }}
📍 Город: {{ $json.contact.city }}
⏰ {{ $json.metadata.time_formatted }}
```

**🔗 Отправить в CRM**
```
Использовать узел: HTTP Request или интеграция CRM
- Bitrix24, Airtable, Salesforce и др.
- Отправьте структурированные данные для автоматизации
```

---

## Шаг 5: Тестирование

### 5.1 Проверка на фронтенде

1. **Откройте сайт** и перейдите к форме "Получить бесплатную консультацию"
2. **Откройте DevTools** (F12) → вкладка Console
3. **Заполните форму** с тестовыми данными
4. **Нажмите "Отправить заявку"**
5. **В консоли вы увидите:**
   ```
   📤 Попытка отправки 1/4...
   📊 JSON для отправки: { ... полный JSON ... }
   ✅ Успешно отправлено в n8n: { success: true }
   ```

### 5.2 Проверка в N8N

1. **Откройте ваш workflow** в n8n
2. **Убедитесь, что workflow активен** (кнопка "Execute Workflow" или toggle включен)
3. **Отправьте тестовую заявку** с сайта
4. **В N8N вы должны увидеть:**
   - Узел Webhook мигнет (получены данные)
   - Появится индикатор выполнения
   - В выходе узла Webhook будут ваши данные

### 5.3 Логирование и отладка

Если данные не приходят:

1. **Проверьте webhook URL** в console:
   ```javascript
   console.log(N8N_CONFIG.webhookUrl)
   ```

2. **Проверьте сетевой запрос:**
   - DevTools → Network tab
   - Заполните и отправьте форму
   - Найдите POST запрос к вашему webhook URL
   - Проверьте status code (должен быть 200)

3. **Проверьте workflow в N8N:**
   - Активен ли он?
   - Правильный ли Path в Webhook узле?
   - Нет ли ошибок при выполнении?

---

## Структура данных JSON

Вот полная структура JSON, которая отправляется на webhook:

```json
{
  "api_version": "1.0",
  "contact": {
    "name": "Иван Петров",
    "phone": "+7 (999) 123-45-67",
    "email": "ivan@example.com",
    "city": "Москва"
  },
  "request": {
    "client_type": "family",
    "service_type": "maintenance",
    "equipment_count": 2,
    "message": "Нужна срочная чистка кондиционера",
    "urgency": "normal"
  },
  "metadata": {
    "form_id": "consultation-form-v1",
    "source": "SAFE AIR Landing Page",
    "user_agent": "Mozilla/5.0...",
    "timestamp": "2025-01-15T14:30:45.123Z",
    "timestamp_unix": 1736873445,
    "date_formatted": "15.01.2025",
    "time_formatted": "14:30:45",
    "timezone": "Europe/Moscow",
    "page_url": "https://safe-air.com",
    "referrer": "google.com"
  }
}
```

### Значения client_type:
- `family` - Частное лицо
- `business` - Компания
- `public` - Государственная организация

### Значения service_type:
- `maintenance` - Техническое обслуживание
- `repair` - Ремонт
- `installation` - Установка
- `diagnostics` - Диагностика
- `cleaning` - Чистка и дезинфекция
- `consultation` - Консультация

---

## Troubleshooting

### ❌ Ошибка: "HTTP 404"
**Причина:** Webhook URL неверный или неактивный workflow

**Решение:**
1. Скопируйте URL еще раз из N8N
2. Проверьте, что workflow активен (toggle включен)
3. Проверьте, что путь (path) совпадает в обоих местах

### ❌ Ошибка: "CORS error"
**Причина:** Cross-Origin запрос заблокирован

**Решение:**
- Если используете самостоятельный N8N, убедитесь что настройки CORS включены
- Либо используйте N8N cloud (там CORS настроены)
- Либо добавьте прокси на бэкенде

### ❌ Ошибка: "Истекло время ожидания (timeout)"
**Причина:** N8N медленно обрабатывает запрос

**Решение:**
1. Увеличьте timeout в конфигурации:
   ```javascript
   timeout: 15000, // вместо 10000
   ```
2. Упростите workflow в N8N (уберите сложные операции)
3. Убедитесь что N8N имеет достаточно ресурсов

### ❌ "N8N webhook URL не установлен"
**Причина:** Используется значение по умолчанию

**Решение:**
- Используйте `window.setN8NWebhook()` из консоли
- ИЛИ отредактируйте `index.html` с реальным URL

### ❌ Данные не приходят в N8N
**Чек-лист:**
1. ✓ Webhook URL скопирован правильно?
2. ✓ Workflow активен в N8N?
3. ✓ Сайт загружается без ошибок (F12 → Console)?
4. ✓ Форма валидна и не показывает ошибки?
5. ✓ Сетевой запрос выполнен (DevTools → Network)?
6. ✓ Статус ответа 200 OK?

### ❌ Форма не отправляется
**Причина:** Ошибка валидации

**Решение:** Проверьте что все обязательные поля заполнены:
- ✓ Имя (минимум 2 символа)
- ✓ Телефон (минимум 10 цифр)
- ✓ Город (минимум 2 символа)
- ✓ Услуга (выбрана из списка)
- ✓ Тип клиента (выбран один из вариантов)

---

## 🔐 Безопасность

### Рекомендации по безопасности:

1. **Включите авторизацию** в Webhook узле N8N:
   - Используйте Basic Auth или Bearer Token
   - Передавайте токен в заголовках (headers)

2. **Валидируйте данные** в первом узле:
   - Проверьте что все поля присутствуют
   - Проверьте форматы (email, phone, и т.д.)
   - Используйте IF узлы для фильтрации

3. **Логируйте запросы:**
   - Добавьте узел Log для отладки
   - Сохраняйте детали в базе данных

4. **HTTPS только:**
   - Всегда используйте HTTPS webhook URLs
   - Никогда не передавайте чувствительные данные в URL параметрах

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте документацию N8N:** https://docs.n8n.io/
2. **Посмотрите примеры:** https://docs.n8n.io/integrations/
3. **Откройте DevTools и проверьте консоль** на ошибки
4. **Проверьте логи N8N** (если self-hosted)

---

## 📚 Дополнительные ресурсы

- 📖 [N8N Webhook Documentation](https://docs.n8n.io/nodes/n8n-nodes-base.webhook/)
- 🎓 [N8N Workflows Guide](https://docs.n8n.io/workflows/)
- 💡 [Common Patterns](https://docs.n8n.io/workflows/advanced-topics/patterns/)
- 🔌 [Available Integrations](https://docs.n8n.io/integrations/list/)

---

**Версия документации:** 1.0
**Дата обновления:** 2025-01-15
**Совместимость:** SAFE AIR v1.0 + N8N 1.0+

Успешной интеграции! 🎉
